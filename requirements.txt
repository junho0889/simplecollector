# ============================================================================
# Simple Collector - Python Dependencies
# ============================================================================

# Core
pyyaml>=6.0.1          # YAML 파싱
python-dotenv>=1.0.0   # 환경변수 관리

# Async / Message Broker
aio-pika>=9.4.0        # RabbitMQ 비동기 클라이언트 (AMQP 0-9-1)

# Industrial Protocols
# pyserial>=3.5        # Modbus RTU 시리얼 모드 (선택, TCP/RTU-over-TCP는 불필요)

# Security
cryptography>=41.0.0   # Fernet 암호화 (RabbitMQ 페이로드, OPC UA 인증서)

# Development (프로덕션에서는 제외)
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# mypy>=1.5.0
# ruff>=0.1.0
